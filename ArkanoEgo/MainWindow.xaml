<Window x:Class="ArkanoEgo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ArkanoEgo"
        mc:Ignorable="d"
        Width="{DynamicResource {x:Static SystemParameters.MaximizedPrimaryScreenWidthKey}}" 
        Height="{DynamicResource {x:Static SystemParameters.MaximizedPrimaryScreenHeightKey}}"
        WindowState="Maximized"
        Title="ArkanoEgo">
    <Grid>
        <!-- frame potrzebne do GamePage itp -->
        <Frame x:Name="gridFrame" Source=".xaml" NavigationUIVisibility="Hidden"/>
        <Button Content="Kreator" HorizontalAlignment="Right" VerticalAlignment="Bottom" Padding="20" Click="Kreator_Click"/>
    </Grid>
</Window>
